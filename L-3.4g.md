# Order L-3.4g — Durability: Leadfinder's memory moves off the deployment disk

Branch: `leadfinder-l34g-durable-db`. Workspace: Leadfinder (Ad-Library-Finder).

## Context, fixed, not to be re-litigated

- The fleet rule, proven on the Chief on August 7: a Reserved VM's disk does NOT survive a publish, so state must never live on a deployment filesystem, VM or not. Evidence points at this app being exposed: its smoke rituals hash a database file, its Activity History showed zero past jobs on August 7 while months should exist, and its emailed result links point at files on the deployment disk. Two of the Chief's written promises hang on this app's memory: same-key adoption of completed jobs, and the ICP backfill (page_url, is_game, found_at) via `lf_job_id`/`lf_lead_id` stored on every Chief prospect.
- **Verify first, in the blast radius, before any change** — the three questions: where this app's state actually lives (engine and file path); whether that file is included in or excluded from the deploy image; and whether the app has published since the two chief jobs ran (job_7nQAfTUr1v and job_u5I0sjFlo_, August 6–7). If reality differs from the exposure story above, halt and report before proceeding.
- Precondition: Michael has provisioned this Repl's managed PostgreSQL so `DATABASE_URL` exists in the workspace. Verify presence; halt and ask if missing.
- The C-3.4c pattern is the template, adapted to this repo's own character: real migrations with explicit recorded state, no silent fallback, boot refuses without a usable database, tests on ephemeral clusters, a persistence-across-restart proof, and the publish-flow gotchas (align the dev database through the app's own mechanism; a generated migration containing DROP is never approved; a paired production database that already holds tables without migration state gets an explicit one-boot adopt path, not a guess).

## Scope

1. **Rescue before anything else, read-only, from the live seam.** Using the workspace `CHIEF_TOKEN` (never printed), page the full lead sets of the two chief jobs from the LIVE deployment via `GET /api/chief/jobs/:id` and `/leads` — the only live calls this order may make, all read-only — and commit them as rescue fixtures in the repo with their job ids, external_ids, and every lead field the seam carries. If either job no longer resolves on live, record that plainly: it is the proof of a wipe having already happened, and the rescue simply captures whatever still exists.
2. **Migrate storage to the managed PostgreSQL**: every table ported one for one through this repo's own migration style, applied at boot through explicit recorded migration state; the server refuses to boot without `DATABASE_URL`; no code path opens the old database file in server mode again, source-shape tested; the boot log names the backend without credentials.
3. **Seed the chief jobs from the rescue fixtures** at first migrated boot, idempotently: after migration, `lf_job_id` and `lf_lead_id` resolution for those two jobs works again, same-key adoption of them still answers correctly over the seam, and the Chief's ICP backfill promise survives. Human job history from the old disk is accepted as lost — users received their results by email and CSV — stated plainly in the ledger rather than silently.
4. **Downloads become database-backed.** Result CSVs and bundles are regenerated on demand from the database at download time, so an emailed link created after this order survives every future publish. Pre-migration links are already broken by past publishes; say so in the ledger, change nothing about them.
5. **Tests** moved to ephemeral clusters per the fleet pattern, all existing suites green on the new backend, plus: the persistence proof across a real process restart; the rescue-seed idempotency; a download link answering identically before and after a simulated restart; and at least one test proven able to fail by reintroducing its defect. Test boots never start the job runner, never call vendors, never send email.
6. **Report**, in chat and the ledger: the three verification answers, the table mapping, what was rescued and what was confirmed lost, the download mechanism, the persistence proof, and the operator notes for the publish (expected migration plan, the adopt path if the paired production database already holds tables, and the standing rule that a generated DROP is never approved).

## Out of scope

O-24 (AppGoblin catalog), O-20/O-21/O-22, any seam contract change, any engine or UI behaviour change beyond downloads becoming database-backed, anything in any other repo.

## Ritual specifics

Blast radius first with the verified answers. Gates: typecheck, tests, build. Godlike audit rounds; security framing must confirm no credentials or tokens in logs, fixtures, or errors, and that the rescue fixtures contain business contact data only, nothing user-private. Smoke, both modes, separate ports, ephemeral clusters, runner disabled: baseline probes byte-identical; the persistence and download proofs across real process boundaries; the seeded chief jobs answering over the seam exactly as live does today. Ledger entry. Merge to main and push when fully clean.

## Hard rules

No destructive git. No force-push. No publishing. No secret changes (`DATABASE_URL` is platform-provisioned). The only live network calls are the read-only rescue GETs in scope item 1. Never start a real scraping or verification run; never send or queue email; no vendor calls; never open the live database file or the live production Postgres from tests or smoke. Halt after three failed attempts on one cause. Ask rather than guess.
